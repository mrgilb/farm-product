import React from "react";
import { StyledInformMessage } from "./styled";


export default function InformMessage ({children}) {
    return (
        <StyledInformMessage>{children}</StyledInformMessage>
    )
};